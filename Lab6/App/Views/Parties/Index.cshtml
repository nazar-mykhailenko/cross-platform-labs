@model List<Party> 

<h1>Parties</h1>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            @foreach (var property in typeof(Party).GetProperties())
            {
                <th>@property.Name</th>
            }
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @foreach (var property in typeof(Party).GetProperties())
                {
                    <td>@property.GetValue(item, null)</td>
                }
                <td>
                    <a asp-action="Details" asp-route-id="@item.PartyId">View Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
